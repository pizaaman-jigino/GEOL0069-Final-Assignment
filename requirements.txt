# AI4EO EuroSAT land-cover classification — Python dependencies
# Tested with Python 3.12 on CPU. Versions are lower bounds known to work.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pandas>=2.0
pillow>=9.5
torch>=2.0          # CPU build is fine: pip install torch --index-url https://download.pytorch.org/whl/cpu
jupyterlab>=4.0
